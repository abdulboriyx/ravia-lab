import assert from "node:assert/strict";
import test from "node:test";
import { rnaCapabilityRegistry, validateRnaCapabilityRegistry, type RnaCapabilityRecord } from "./rna-capability-registry.ts";
test("F5-B registers every current RNA capability deterministically", () => { assert.deepEqual(validateRnaCapabilityRegistry(rnaCapabilityRegistry), { valid: true, issues: [] }); assert.equal(rnaCapabilityRegistry.length, 12); for (const id of ["rna-generic-structure", "rna-types-functions", "rna-nascent-transcript", "rna-processing", "rna-secondary-structure", "rna-base-pairing", "rna-dna-hybridization", "rna-cleavage", "rna-exonuclease-degradation", "rna-chemical-stability", "rna-local-chemistry", "rna-dna-chemistry-comparison"]) assert.ok(rnaCapabilityRegistry.some((record) => record.capabilityId === id)); });
test("F5-B rejects vocabulary drift, renderer owners, duplicate IDs, and malformed readiness", () => { const invalid = structuredClone(rnaCapabilityRegistry) as RnaCapabilityRecord[]; invalid[0]!.capabilityId = invalid[1]!.capabilityId; invalid[0]!.requiredSemanticEntities = ["renderer-mesh" as never]; invalid[0]!.presentationOwner = "ThreeScene" as never; invalid[0]!.readiness.export = "video-only" as never; const result = validateRnaCapabilityRegistry(invalid); assert.equal(result.valid, false); if (!result.valid) assert.ok(result.issues.some((issue) => issue.message.includes("unique")) && result.issues.some((issue) => issue.path.includes("requiredSemanticEntities")) && result.issues.some((issue) => issue.path.includes("presentationOwner")) && result.issues.some((issue) => issue.path.includes("readiness.export"))); });
